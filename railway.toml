# Railway: connect GitHub repo → Deploy
# Add a volume mounted at /app/.data for projects & exports.
# Set env vars in the Railway dashboard (see .env.example).

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
